package com.lesehankoding.library

/**
 * Flick intro slide selection listener
 *
 * @constructor Create empty Flick intro slide selection listener
 */
interface FlickIntroSlideSelectionListener {
    /**
     * On slide selected
     *
     */
    fun onSlideSelected()

    /**
     * On slide deselected
     *
     */
    fun onSlideDeselected()
}
